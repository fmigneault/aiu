======================================
aiu: Audio Info Updater
======================================

Build package
=============

At the command line::

    $ conda create -n aiu
    $ source activate aiu
    $ cd `<git-root-dir>`
    $ pip install .

Running
=============

At the command line::

    $ python miu.py <args>


